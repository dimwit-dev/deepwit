rules = [
  RemoveUnused
]

// On Scala 3 RemoveUnused only handles imports; the other categories are reported
// by the compiler under -Wunused:all but left for a human to judge.
RemoveUnused {
  imports = true
  privates = false
  locals = false
  params = false
  patternvars = false
}
